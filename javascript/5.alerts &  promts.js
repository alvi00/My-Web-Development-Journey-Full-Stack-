// Printing a warning message to the console
console.warn("This is a warning");

// Printing an error message to the console
console.error("This is an error");

// Asking the user to input their first name through a prompt dialog
let firstname = prompt("Enter your first name");

// Asking the user to input their last name through a prompt dialog
let finalname = prompt("Enter your last name");

// Concatenating the first name and last name input by the user
let msg = (firstname + " " + finalname);

// Displaying a welcome message along with the concatenated full name
alert("Welcome " + msg);
